spring:
  application:
    name: campus-beats-backend
  
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/campus_beats?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true}
    username: ${SPRING_DATASOURCE_USERNAME:root}
    password: ${SPRING_DATASOURCE_PASSWORD:Swaraj@0405}
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
  
  # Email Configuration
  mail:
    host: smtp.gmail.com
    port: 587
    username: paramjitbaral44@gmail.com
    password: lonx cnlv kyiz iaui
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 3000
          writetimeout: 5000

server:
  port: ${PORT:8081}

# JWT Configuration
jwt:
  secret: campusBeatsSecretKey2024ForJWTTokenGenerationWithExtraLengthToMeetSecurityRequirementsForHS256Algorithm
  expiration: 86400000 # 24 hours in milliseconds

# CORS Configuration
cors:
  # Add common local dev origins so multiple frontends can use this backend during development
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000,http://localhost:4000,http://localhost:3001}
  allowed-methods: GET,POST,PUT,DELETE,OPTIONS
  allowed-headers: "*"
  allow-credentials: true

# File Upload Configuration
file:
  upload:
    dir: ./uploads/
    max-size: 50MB

# WebSocket Configuration
websocket:
  # Mirror CORS origins for websocket usage during local development
  allowed-origins: ${WS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000,http://localhost:4000,http://localhost:3001}

logging:
  level:
    com.campusbeats: DEBUG
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG